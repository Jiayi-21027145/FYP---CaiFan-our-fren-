
    // Modify the next line to convert the list to proper type
   // List<LocationPrice> locationP = (ViewData["locationP"] as List<LocationPrice>)!;

    // Modify the next line to convert the list to proper type
   // List<Location> location = (ViewData["location"] as List<Location>)!;
//}
//<div>
  //  @{
       // var sequence1 = locationP
      //  .Select(lp => new
      //  {
      //      locations = "Location",
           
      //  })
      //  .ToList();


     //   var sequence2 = location
     //   .Select(l => new
     //   {
     //       prices = "Price"
       
     //   })
     //   .ToList();

    //    var unionSeq = sequence2
      //  .Union(sequence1)
        //.ToList();
   // }
