@model List<FYP5.Models.LocationItemPriceViewModel>

@{
    Layout = "_CaiFan"; // Replace with your actual layout if different
}

<h2>Summary of Prices by Location</h2>

@* <table class="table">
    <thead>
        <tr>
            <th>Location Name</th>
            <th>Total Price</th>
        </tr>
    </thead>
    <tbody>
        @if (Model != null && Model.Any())
        {
            foreach (var item in Model)
            {
                <tr>
                    <td>@Html.DisplayFor(modelItem => item.LocationId)</td>
                    <td>@Html.DisplayFor(modelItem => item.Price)</td>
                </tr>
            }
        }
        else
        {
            <tr>
                <td colspan="2">No data to display.</td>
            </tr>
        }
    </tbody>
</table>
 *@

@* <table class="table">
    <thead>
        <tr>
            <th>Location ID</th>
            <th>Item ID</th>
            <th>Price</th>
        </tr>
    </thead>
    <tbody>
        @foreach (var lp in Model.)
        {
            <tr>
                <td>@lp.LocationId</td>
                <td>@lp.ItemId</td>
                <td>@lp.Price</td>
            </tr>
        }
    </tbody>
</table> *@
<table class="table">
    <thead>
        <tr>
            <th>Location Name</th>
            <th>Item Name</th>
            <th>Price</th>
        </tr>
    </thead>
    <tbody>
        @foreach (var lp in Model)
        {
            <tr>
                <td>@lp.LocationName</td>
                <td>@lp.ItemName</td>
               <td>@(lp.Price ?? 0).ToString("C")</td>
 @* Format as currency *@
            </tr>
        }
    </tbody>
</table>